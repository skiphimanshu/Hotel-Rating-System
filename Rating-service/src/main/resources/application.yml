eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    
server:
  port: 8598
spring:
  application:
    name: RATING-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Himanshu
    url: jdbc:mysql://localhost/ratingservice
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
